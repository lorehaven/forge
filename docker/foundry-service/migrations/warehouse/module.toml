name = "warehouse"
version = "0.2.1"
description = "Warehouse - storage service; authenticates against the shared realm"
default_schema = "warehouse"
requires = ["auth@^0.2", "quench-core@^0.1"]

[variables]
# Realm schema that owns users; keep in sync with the auth module.
auth_schema = "auth"
