name = "auth"
version = "0.2.1"
description = "Shared identity realm - the estate's users, owned by gatehouse"

# One instance per database, never per service schema: the whole point is that
# sage, switchboard and warehouse authenticate against the same users.
scope = "database"
default_schema = "auth"
