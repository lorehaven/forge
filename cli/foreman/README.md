# Foreman

Foreman runs a project's local development estate from a single `foreman.toml` file instead of a pile of shell scripts. It starts services, the Docker containers underneath them, and the tasks that must run before either, in dependency order, and gets out of the way otherwise.

See [docs/cli/foreman.md](../../docs/cli/foreman.md) for full documentation.
